$('#edit-page-button').hide();

var $pageContent = $('#page-content');
// oldHtml = $pageContent.html();
$pageContent.html("<%= (escape_javascript render partial: 'form', locals: { page: @page, remote: true }) %>");

// $(window).on('beforeunload.page', unsavedChangesToPage);
